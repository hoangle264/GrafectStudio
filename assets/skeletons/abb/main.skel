MODULE {MODULE_NAME}
    ! ============================================================
    ! Generated by GrafectStudio Skeleton Engine — ABB RAPID
    ! Date   : {DATE}
    ! WARNING: Teach ALL positions before running in AUTO mode!
    ! ============================================================

    ! --- I/O Signal Aliases ---
{INJECT:signal_decls}

    ! --- Position Targets (DEFAULT VALUES — MUST BE TAUGHT) ---
{INJECT:position_decls}

    ! --- Tool Data (DEFAULT TCP — MUST BE CALIBRATED) ---
{INJECT:tool_decls}

    PROC main()
        ! === INITIALIZATION ===
{INJECT:init}

        ! === MAIN SEQUENCE LOOP ===
        WHILE TRUE DO
{INJECT:steps}
        ENDWHILE

        ! === ERROR / E-STOP HANDLERS ===
{INJECT:labels}
    ENDPROC
ENDMODULE
